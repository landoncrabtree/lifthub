# Domain for Caddy auto-SSL. Use "localhost" for local development.
# Set to your real domain (e.g. gym.example.com) for production — Caddy
# will automatically provision and renew Let's Encrypt certificates.
DOMAIN=localhost

# Secret used to sign JWT tokens. Change this to a random string in production.
JWT_SECRET=change-me-in-production
